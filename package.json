{
  "name": "draftpine",
  "version": "2.0.0",
  "description": "Agent-extensible static wireframe compiler and evaluator.",
  "type": "module",
  "bin": {
    "draftpine": "./dist/src/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:e2e": "vitest run tests/integration",
    "draftpine": "tsx src/cli/index.ts",
    "prepack": "pnpm build"
  },
  "dependencies": {
    "@playwright/test": "^1.56.1",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  },
  "engines": {
    "node": ">=20"
  }
}
