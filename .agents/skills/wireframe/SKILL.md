---
name: wireframe
description: Build or revise a minimal Pico CSS + Alpine.js Draftpine wireframe screen or browsable prototype from a product idea, screen packet, rough requirements, or screenshot.
---

Read and follow `AGENTS.md`, then use `agent-workflows/wireframe.md`.

Before finishing, run:

```bash
python3 scripts/check.py --runtime --json
```

