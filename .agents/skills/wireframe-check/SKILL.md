---
name: wireframe-check
description: Run Draftpine's checker, read next_actions, and repair the wireframe until all errors pass.
---

Read and follow `AGENTS.md`, then use `agent-workflows/check-and-repair.md`.

Run:

```bash
python3 scripts/check.py --runtime --json
```

