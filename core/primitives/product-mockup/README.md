# core.productMockup

Bounded product UI artifact for marketing and app pages.

Use it when the page needs to show concrete workflow state, schedule state, lists, queues, or product chrome instead of a generic illustration.

Slots:

- `title`: required string
- `description`: text
- `toolbar`: array of small controls
- `panels`: required array of product state panels
- `activity`: array of timeline rows
